

if message.content.startswith (':joy:'): #recieve Command
    #pass the string to SoundBoard class
    #then that passes the string to the database
    #retrieve audio
    #
    await message.channel.send ('silly_sound'): #place holder